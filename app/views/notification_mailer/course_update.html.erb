<%= p_tag "Olá, #{@receiver.first_name}." %>
<%= p_tag "<strong>#{@sender.full_name}</strong> editou as informações do curso <strong>#{@course.title}</strong>!" %>
<%= p_tag a_tag('Clique aqui para conferir.', dashboard_course_url(@course, subdomain: @school.subdomain)) %>