<!DOCTYPE html>
<html>
<head>
  <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
</head>
<body style="margin:0;background-color:#cccccc;">
  <br>

  <table align="center" cellspacing="0" cellpadding="0" border="0" width="540" style="background-color:#fff;padding:10px;border:1px solid #888888;font-size:12px;color:#3d4b4b;font-family:Helvetica,Arial,sans-serif;">
    <thead>
      <tr>
        <td style="text-align:center;background-color:#FAFAFA;margin-bottom: 10px; 1px solid #333;">
          <% @school = School.find_by_id(School.current_id) %>
					<% if @school.try(:layout_configuration).try(:site_logo).try(:present?) %>
						<%= image_tag @school.layout_configuration.site_logo.url(:medium) %>
					<% elsif @school.present? %>
            <h1 style="margin-top: 20px;"><%= @school.name %></h1>
          <% else %>
            <%= image_tag('edools_logo_v4.png', :alt=>"Edools", width: "150", style: "padding: 13px 0;") %>
					<% end %>
        </td>
      </tr>
    </thead>
    <tbody>
      <tr style="vertical-align:top;">
        <td style="padding:30px 10px;background-color:#fff;min-height:200px;">
          <%= yield %>
        </td>
      <tr>
    </tbody>
    <tfoot>
		<!-- <tr>
					<td>
							<hr style="border:0;border-bottom:1px solid #aaa;" />

							<p style="color:#666;font-family:sans-serif;font-size:12px;">
							  Caso tenha problemas, não hesite em enviar sua pergunta para nossa equipe de
							  suporte em
							  <a style="color:#e9490f;" href="mailto:support@startupbase.net">support@startupbase.net</a>.
							</p>
						</td>
					</tr> -->
      <tr>
        <hr>
        <td style="padding:8px 10px 5px 10px;margin-top: 20px;">
          <% if @school.present? && @school.module_configuration.inactive?(:remove_edools_logo) %>
  					<%= link_to image_tag('edools_logo_v4.png', :alt=>"Edools", width: "100"), root_url(subdomain: "www"), :title => "Edools", :style=>"float:right;" %>
            <p style="color:#000;font-family:sans-serif;font-size:14px;float:right;margin-top: 10px;margin-right: 4px;">Plataforma de ensino por</p>
          <% end %>
        </td>
      </tr>
    </tfoot>
  </table>
</body>
</html>