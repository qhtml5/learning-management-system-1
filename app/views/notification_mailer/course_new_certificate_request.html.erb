<%= p_tag "Olá, #{@receiver.first_name}." %>
<%= p_tag "<strong>#{@sender.full_name}</strong> (email: #{@sender.email}) solicitou o certificado do curso <strong>#{@course.title}</strong>" %>
<%= p_tag a_tag('Clique aqui para emitir o certificado.', dashboard_students_url(subdomain: @school.subdomain)) %>