<% @students.last(14).each do |student| %>
	<div style="margin: 1%; padding-top: 5px; float: left;" class="hint hint--left" data-hint="<%= student.full_name %>"><%= image_tag(student.image.url(:thumb), size: "40x40", class: "img-circle img-student") %></div>
<% end %>
<div class="clearfix"></div>