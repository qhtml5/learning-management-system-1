<%= p_tag "Olá, #{@receiver.first_name}." %>
<%= p_tag "<strong>#{@sender.full_name}</strong> editou a ementa do curso <strong>#{@course.title}</strong>!" %>
<%= p_tag a_tag('Clique aqui para conferir.', dashboard_course_lessons_url(@course, subdomain: @school.subdomain)) %>
